{
  "build": { "dockerfile": "Dockerfile" },
  "forwardPorts": [3000],
  "postCreateCommand": "jupyter lab --ip=0.0.0.0 --ServerApp.token='development' --ServerApp.password='development' --allow-root --no-browser",
}
